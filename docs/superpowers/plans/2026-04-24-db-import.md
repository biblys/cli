# db:import Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter la commande `biblys db:import <site>` qui fait un dump de la BDD de prod via SSH et l'importe dans une BDD locale `biblys-{site_name}`.

**Architecture:** La commande crée un fichier temporaire `/tmp/biblys-{site}.sql` via SSH mysqldump (étape 1, compteur de Mo), puis l'importe dans MySQL local avec une barre de progression (étape 2), et supprime le fichier temporaire dans un bloc `finally`. Les credentials distants sont récupérés via `fetchCredentials` (cache SQLite existant).

**Tech Stack:** TypeScript, execa v7, Node.js streams (pipe + data events), chalk, fs.createWriteStream / fs.createReadStream.

---

> **Note :** Ce projet n'a pas de tests automatisés. Les étapes de vérification sont des compilations TypeScript et des tests manuels.

---

### Task 1 : Créer `src/commands/dbImport.ts`

**Files:**
- Create: `src/commands/dbImport.ts`

- [ ] **Étape 1 : Créer le fichier avec le contenu suivant**

```typescript
import fs from 'fs';
import os from 'os';
import path from 'path';

import { execa } from 'execa';
import chalk from 'chalk';

import { getCliConfigForSite } from '../services/CliConfigService.js';
import { fetchCredentials } from '../services/revenue.js';

export default async function dbImportCommand(siteName: string): Promise<void> {
  const site = getCliConfigForSite(siteName);
  if (!site) {
    process.exit(1);
  }

  process.stdout.write(`${chalk.yellow('⇢')} Récupération des credentials pour ${chalk.blue(siteName)}…\n`);
  const creds = await fetchCredentials(site);

  const dbName = `biblys-${site.name}`;
  process.stdout.write(`${chalk.yellow('⇢')} Création de la base ${chalk.blue(dbName)}…\n`);
  await execa('mysql', ['-u', 'root', '-e', `DROP DATABASE IF EXISTS \`${dbName}\`; CREATE DATABASE \`${dbName}\``]);

  const tmpFile = path.join(os.tmpdir(), `biblys-${site.name}.sql`);

  try {
    // Étape 1 : dump via SSH
    const passArg = creds.pass ? `-p'${creds.pass}'` : '';
    const dumpCmd = `mysqldump -h ${creds.host} -P ${creds.port} -u ${creds.user} ${passArg} ${creds.baseName} 2>/dev/null`;
    const sshProc = execa('ssh', [site.server, dumpCmd]);
    const writeStream = fs.createWriteStream(tmpFile);
    let bytesReceived = 0;

    sshProc.stdout!.on('data', (chunk: Buffer) => {
      bytesReceived += chunk.length;
      const mb = (bytesReceived / 1024 / 1024).toFixed(1);
      process.stdout.write(`\r${chalk.yellow('⇢')} [1/2] Dump de ${chalk.blue(creds.baseName)}… ${mb} Mo`);
    });

    sshProc.stdout!.pipe(writeStream);

    await Promise.all([
      new Promise<void>((resolve, reject) => {
        writeStream.on('finish', resolve);
        writeStream.on('error', reject);
      }),
      sshProc,
    ]);

    process.stdout.write('\n');
    const dumpMb = (bytesReceived / 1024 / 1024).toFixed(1);
    console.log(`${chalk.green('✓')} [1/2] Dump terminé (${dumpMb} Mo)`);

    // Étape 2 : import local
    const fileSize = fs.statSync(tmpFile).size;
    const readStream = fs.createReadStream(tmpFile);
    const mysqlProc = execa('mysql', ['-u', 'root', dbName]);
    let bytesRead = 0;

    readStream.on('data', (chunk: Buffer) => {
      bytesRead += chunk.length;
      const pct = Math.floor((bytesRead / fileSize) * 100);
      const filled = Math.floor(pct / 10);
      const bar = '█'.repeat(filled) + '░'.repeat(10 - filled);
      process.stdout.write(`\r${chalk.yellow('⇢')} [2/2] Import dans ${chalk.blue(dbName)}… [${bar}] ${pct}%`);
    });

    readStream.pipe(mysqlProc.stdin!);
    await mysqlProc;

    process.stdout.write('\n');
    console.log(`${chalk.green('✓')} [2/2] Import terminé`);
    console.log(`${chalk.green('✓')} Base ${chalk.blue(dbName)} importée avec succès`);

  } catch (err: unknown) {
    process.stdout.write('\n');
    const message = err instanceof Error ? err.message : String(err);
    console.error(`${chalk.red('✗')} Erreur : ${message}`);
    process.exit(1);
  } finally {
    if (fs.existsSync(tmpFile)) {
      fs.unlinkSync(tmpFile);
    }
  }
}
```

- [ ] **Étape 2 : Compiler pour vérifier qu'il n'y a pas d'erreurs TypeScript**

```bash
npm run build
```

Résultat attendu : compilation sans erreur, `build/src/commands/dbImport.js` créé.

- [ ] **Étape 3 : Committer**

```bash
git add src/commands/dbImport.ts
git commit -m "feat: add db:import command"
```

---

### Task 2 : Enregistrer la commande dans `app.ts`

**Files:**
- Modify: `app.ts`

- [ ] **Étape 1 : Ajouter l'import en haut du fichier, après les imports existants**

Après la ligne :
```typescript
import adminsCommand from './src/commands/admins.js';
```

Ajouter :
```typescript
import dbImportCommand from './src/commands/dbImport.js';
```

- [ ] **Étape 2 : Enregistrer la commande dans le CLI Yargs**

Après le bloc de la commande `admins` (avant la commande `$0`) :

```typescript
  // @ts-ignore
  .command('db:import <site>', 'importe la base de données de prod dans une base locale', (yargs) => {
    return yargs
      .positional('site', {
        describe: 'nom du site à importer',
        type: 'string',
      })
  }, async ({ site }: { site: string }) => {
    await dbImportCommand(site);
  })
```

- [ ] **Étape 3 : Compiler pour vérifier qu'il n'y a pas d'erreurs TypeScript**

```bash
npm run build
```

Résultat attendu : compilation sans erreur.

- [ ] **Étape 4 : Vérifier que la commande apparaît dans l'aide**

```bash
node build/app.js --help
```

Résultat attendu : `db:import <site>` listé parmi les commandes disponibles avec la description « importe la base de données de prod dans une base locale ».

- [ ] **Étape 5 : Committer**

```bash
git add app.ts
git commit -m "feat: register db:import command in CLI"
```

---

### Task 3 : Test manuel de bout en bout

- [ ] **Étape 1 : Tester avec un site inexistant**

```bash
node build/app.js db:import sitequisexistepas
```

Résultat attendu :
```
✗ Site sitequisexistepas not found
```
Avec code de sortie non-zéro.

- [ ] **Étape 2 : Lancer l'import sur un site réel (ex. charybde)**

```bash
node build/app.js db:import charybde
```

Résultat attendu (succession de messages) :
```
⇢ Récupération des credentials pour charybde…
⇢ Création de la base biblys-charybde…
⇢ [1/2] Dump de charybde_biblys… 245.0 Mo
✓ [1/2] Dump terminé (245.0 Mo)
⇢ [2/2] Import dans biblys-charybde… [██████████] 100%
✓ [2/2] Import terminé
✓ Base biblys-charybde importée avec succès
```

- [ ] **Étape 3 : Vérifier que la BDD locale existe et contient les données**

```bash
mysql -u root -e "SHOW DATABASES LIKE 'biblys-charybde';"
mysql -u root biblys-charybde -e "SHOW TABLES;" | head -20
```

Résultat attendu : la base `biblys-charybde` existe et contient les tables Biblys (ex. `stock`, `payments`, etc.).

- [ ] **Étape 4 : Vérifier que le fichier temporaire a été supprimé**

```bash
ls /tmp/biblys-charybde.sql 2>&1
```

Résultat attendu : `No such file or directory`.

- [ ] **Étape 5 : Installer globalement et relancer pour vérifier**

```bash
npm install -g . && biblys db:import charybde
```

Résultat attendu : même comportement qu'à l'étape 2.
